{
  "name": "herdr-plugin-simple-dispatch",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node --check controller.js && node --check workflow.js && node --check cleanup.js && node --check prompts.js && node --check harnesses.js && node --test"
  }
}
